<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:backUp="clr-namespace:Chiota.PageModels.BackUp"
             xmlns:bindable="clr-namespace:Chiota.Controls.Bindable;assembly=Chiota"
             x:Class="Chiota.Pages.BackUp.WriteSeedPage"
             Title="Write seed down">

    <ContentPage.BindingContext>
        <backUp:WriteSeedPageModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Style="{DynamicResource BaseBackgroundStackLayout}">
            <StackLayout HorizontalOptions="Center" VerticalOptions="FillAndExpand">
                <StackLayout HorizontalOptions="Center" VerticalOptions="CenterAndExpand" Spacing="32">
                    <StackLayout HorizontalOptions="Center" VerticalOptions="Start" Spacing="24">
                        <!-- Logo of the iota chat app-->
                        <Image Style="{DynamicResource ChiotaLogo}" HorizontalOptions="Center" HeightRequest="128" WidthRequest="128"/>
                        <Label Style="{DynamicResource DescriptionLabel}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Please write your "/>
                                    <Span Text="seed" FontAttributes="Bold"/>
                                    <Span Text=" down and "/>
                                    <Span Text="keep him safe" FontAttributes="Bold"/>
                                    <Span Text="."/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>

                    <StackLayout VerticalOptions="CenterAndExpand">
                        <StackLayout VerticalOptions="Center" Spacing="24">
                            <Frame Style="{DynamicResource RoundedFrame8}" VerticalOptions="Center" BackgroundColor="#321d4999">
                                <StackLayout HorizontalOptions="Center" VerticalOptions="Center" HeightRequest="104" Padding="8">
                                    <ScrollView>
                                        <bindable:BindableStackLayout Items="{Binding Seed}" Spacing="4"/>
                                    </ScrollView>
                                </StackLayout>
                            </Frame>
                            <Label Style="{DynamicResource Label14}" HorizontalTextAlignment="Center" Text="Swipe down, to see the full seed." Opacity="0.6"/>
                        </StackLayout>
                    </StackLayout>
                </StackLayout>

                <StackLayout HorizontalOptions="Center" VerticalOptions="End" Spacing="8" Padding="0, 0, 0, 24">
                    <Button Style="{DynamicResource RoundedButton16}" Text="Continue" WidthRequest="256" Command="{Binding ContinueCommand}"/>
                </StackLayout>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>